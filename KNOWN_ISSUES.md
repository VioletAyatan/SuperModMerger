# 已知问题与可靠性边界

本文记录当前版本中已经由源码检查或构建验证确认的问题，供发布评估、修复排期和回归测试使用。

- 检查基线：`7211957`（2026-08-12）
- 最近复核：2026-08-13
- 状态含义：`未修复` 表示当前主分支仍可到达该问题；`设计中` 表示已有接口或方案，但尚未形成完整运行链路。

## 发布结论

当前版本可以构建并启动 CLI，但尚不适合无保护地处理不可信归档或直接覆盖唯一一份游戏目录。正式发布前至少应解决 SMM-001、SMM-002、SMM-003、SMM-004 和 SMM-005，并建立相应的自动化回归测试。

建议当前使用者：

1. 只处理来源可信的 MOD。
2. 在游戏目录副本上运行，并备份已有 `data7.pak`。
3. 明确记录 MOD 顺序；不要把重复运行结果默认视为确定结果。
4. 检查最终错误汇总及生成 PAK 的条目，而不只依据“创建成功”提示。
5. 对删除代码、自动路径纠正及失败回退的文件进行人工复核。

## 严重度定义

| 级别 | 含义 |
|---|---|
| P0 | 可能越界写文件、破坏已有输出，或构成发布阻断的安全问题 |
| P1 | 可能产生错误、不完整或不可重复的合并结果 |
| P2 | 功能边界、可观测性、构建和维护性问题 |

## 问题清单

### SMM-001：归档条目可逃出临时解压目录

- 严重度：P0
- 状态：未修复
- 位置：`ExtractArchiveCallback.getStream()`
- 现状：代码执行 `outputDir.resolve(fileEntryName).normalize()` 后直接创建目录并写入，没有验证规范化后的路径仍以 `outputDir` 为根。
- 影响：恶意或异常归档中的 `../`、绝对路径或等价路径可能写到临时目录之外。该链路覆盖 PAK、ZIP、7z、RAR 及递归内嵌归档。
- 修复验收：拒绝所有逃出解压根目录的条目；补充相对穿越、绝对路径、混合分隔符和嵌套归档测试。

### SMM-002：并行解包导致 MOD 累计顺序不确定

- 严重度：P1
- 状态：未修复
- 位置：`ModExtractor.extractAllMods()`、`FileMergerEngine.mergeFiles()`
- 现状：MOD 使用 `parallelStream()` 解包，文件来源按并行完成顺序写入并发列表；后续合并按该列表顺序累计。
- 影响：相同输入重复运行时，冲突出现顺序、自动选择、失败回退对象及最终内容可能不同。
- 修复验收：扫描时为每个 MOD 分配稳定序号；分组后按序号排序；同一输入连续运行的输出条目及内容哈希完全一致。

### SMM-003：输出 PAK 不是事务式写入

- 严重度：P0
- 状态：未修复
- 位置：`PakManager.createPak()`
- 现状：压缩流直接在最终输出路径创建，现有 `data7.pak` 可能在新包验证完成前被截断或覆盖。
- 影响：磁盘空间不足、权限错误、单个文件读取失败或进程中断时，可能同时失去旧输出和新输出。
- 修复验收：先写同目录临时文件；重新打开并验证 ZIP 条目和 CRC；成功后原子替换目标；失败时保留原文件。

### SMM-004：缺少节点不会形成删除操作

- 严重度：P1
- 状态：设计中
- 位置：`TechlandScrFileMerger.deepCompare()`、XML/JSON 对应比较器、`DeletionRecord`
- 现状：SCR 比较只遍历 incoming MOD 中存在的节点。`DeletionRecord` 和 `resolveDeletionConflicts()` 已定义，但当前合并器没有创建删除记录。
- 影响：作者通过删除语句或注释掉语句表达的删除意图无法应用；简单地把“缺少节点”视为删除又会误删旧版本 MOD 不知道的新原版节点。
- 修复验收：优先识别显式删除标记和可解析的注释语句；使用 Vanilla/Accumulated/Incoming 三方规则；无证据缺失默认保留或要求确认；增加删除、修改-删除和旧版本缺失 golden tests。

### SMM-005：文件合并失败后仍可能发布成功结果

- 严重度：P1
- 状态：未修复
- 位置：`FileMergerEngine.mergeAllFiles()`、`FileMergerEngine.mergeFiles()`
- 现状：单文件异常会被记录后继续；语义合并失败时默认复制最后一个 MOD 的版本。最终仍会打包并显示创建成功。
- 影响：用户可能把包含静默回退、遗漏文件或未解决错误的 PAK 当作完整结果。
- 修复验收：任务状态区分成功、带回退完成和失败；高严重度错误默认阻止打包；若允许继续，必须由用户明确确认并在退出码、摘要和日志中标记。

### SMM-006：原版路径索引按 basename 单值覆盖

- 严重度：P1
- 状态：未修复
- 位置：`BaseModManager.indexBasePack()`
- 现状：原版文件通过文件名映射到单个完整路径；不同目录中的同名文件以及不同官方 PAK 中的同名文件会互相覆盖。
- 影响：路径自动纠正可能把文件移动到错误目录，Vanilla 基线也可能选错来源。
- 修复验收：一个文件名保留全部候选；定义 `data0/data1/databt` 优先级；结合完整路径和父目录判断；存在歧义时禁止静默纠正。

### SMM-007：递归解包没有资源限制

- 严重度：P1
- 状态：未修复
- 位置：`PakManager.extractRecursive()`、`ExtractArchiveCallback`
- 现状：没有限制嵌套深度、条目数量、单文件大小、总展开大小、压缩比或处理时间。
- 影响：大型归档、深层嵌套和压缩炸弹可能耗尽磁盘、内存或处理线程。
- 修复验收：在统一归档层实施可配置配额；超限时停止当前来源、记录完整嵌套链和拒绝原因；增加资源上限测试。

### SMM-008：部分 MOD 资源会被主动跳过

- 严重度：P2
- 状态：未修复
- 位置：`ModExtractor.shouldSkipFile()`
- 现状：`.txt`、`.md`、`.dll`、`.asi`、`.exe`、`.rpack`、`.dds` 不进入最终 PAK。
- 影响：安全拒绝可执行文件是合理策略，但 `.rpack`、`.dds` 可能是 MOD 必需资源；当前输出不能保证完整包含输入 MOD 的全部内容。
- 修复验收：区分“安全拒绝”“仅作说明文件”“游戏资源”；在结果摘要中列出所有跳过条目；必要资源提供明确的复制或人工处理策略。

### SMM-009：默认测试套件依赖个人路径和缺失样例

- 严重度：P1
- 状态：未修复
- 位置：`TechlandSyntaxTest`
- 现状：2026-08-13 执行 `mvn test` 共 10 项，7 项通过、3 项因硬编码路径或缺失文件报错：
  - `C:\Users\Administrator\Desktop\data7`
  - `D:\SteamLibrary\steamapps\common\Dying Light 2\ph\source\data0.pak`
  - `examples\frame_script_game.scr`
- 影响：新环境无法得到绿色默认构建；现有通过项也不能代表核心多 MOD 合并行为已有系统覆盖。
- 修复验收：所有测试使用仓库内小型 fixture 或临时目录；默认 `mvn test` 无外部游戏安装依赖；SCR/XML/JSON 均具备输入到期望输出的 golden tests。

### SMM-010：CLI 文档、自动检测和运行行为尚未收口

- 严重度：P2
- 状态：未修复
- 位置：`README*`、`AppMain.locateBaseModPath()`、`GameDetector`
- 现状：`-b` 实际接收包含 `data0.pak`、`data1.pak` 或 `databt.mpak` 的目录，而部分文档和示例仍写成单个 PAK 路径；`GameDetector` 已实现但入口没有调用。
- 影响：用户按示例传参可能直接失败；已存在的自动检测能力无法改善首次使用体验。
- 修复验收：统一中英文参数说明和示例；决定并测试 `-b` 的唯一契约；接入或删除未使用的自动检测逻辑。

## 当前构建快照

- `mvn -DskipTests package`：成功。
- `java -jar target/SuperModMerger-1.7.1-all.jar --help`：成功。
- `mvn test`：7 项通过、3 项环境错误，默认测试未通过。
- 构建仍报告重复源码根和 Shade 重叠资源警告，当前不会阻止生成 JAR，但应在构建配置清理时处理。

## 建议修复顺序

1. SMM-001：建立归档路径根边界。
2. SMM-003：改为事务式输出。
3. SMM-002：固定扫描、分组和累计合并顺序。
4. SMM-005：建立可信的任务结果和退出状态。
5. SMM-004：完成保守的删除意图链路。
6. SMM-009：把默认测试改为完全自包含。
7. SMM-006、SMM-007：完善路径候选和资源配额。
8. SMM-008、SMM-010：收紧产品边界和文档契约。
